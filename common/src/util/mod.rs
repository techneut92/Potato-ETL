/// Shared Arrow utilities (no I/O dependencies).
pub mod arrow;
/// Schema-mapping and column-override helpers (parse_arrow_type).
pub mod schema;
/// Step-level debug logging: schema display and data head.
pub mod debug;
